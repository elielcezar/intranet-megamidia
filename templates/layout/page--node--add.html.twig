{#
/**
 * @file
 * Theme override for node add pages.
 *
 * Simplified layout focused on content creation forms.
 * Removes sidebar to provide more space for creating content.
 * Perfect for content creation workflows.
 */
#}

<div class="add-node-page layout-container flex h-screen bg-gray-50 overflow-hidden font-sans">

  {# Mobile Overlay #}
  <div class="js-sidebar-overlay sidebar-overlay hidden md:hidden"></div>

  {# Sidebar #}
  {% include '@intranet_megamidia/includes/sidebar.html.twig' %}

  {# Main Content Wrapper - Full Width #}
  <div class="layout-main-wrapper flex-1 flex flex-col min-w-0 overflow-hidden">

    {# Header - Simplified for creating #}
    {% include '@intranet_megamidia/includes/header.html.twig' %}

    {# Main Content - Full Width for Creating #}
    <main id="main-content" class="layout-content flex-1 overflow-y-auto p-4 md:p-8 scroll-smooth">
      <div class="layout-content-inner max-w-4xl mx-auto">

        {# Breadcrumb / Context #}
        <div class="mb-6">
          <nav class="flex items-center space-x-2 text-sm text-gray-600">
            <a href="{{ path('<front>') }}" class="hover:text-brand-600 transition-colors">{{ 'Início'|t }}</a>
            <span class="text-gray-400">/</span>
            <span class="text-gray-900 font-medium">{{ node_type_label|default('Novo Conteúdo'|t) }}</span>
          </nav>
        </div>

        {# Page Title Block #}
        {% if page.title %}
          <div class="mb-6">
            {{ page.title }}
          </div>
        {% endif %}

        {# Page Title - Dynamic based on content type #}
        <div class="mb-8">
          <h1 class="text-3xl font-bold text-gray-900">
            {{ 'Criar'|t }} {{ node_type_label|default('Novo Conteúdo'|t) }}
          </h1>
          <p class="text-gray-600 mt-2">
            {{ 'Preencha os campos abaixo para criar seu conteúdo. Campos marcados com * são obrigatórios.'|t }}
          </p>
        </div>

        {# Messages #}
        {% if page.highlighted %}
          <div class="mb-6">
            {{ page.highlighted }}
          </div>
        {% endif %}

        {# Help Region #}
        {% if page.help %}
          <div class="mb-6">
            {{ page.help }}
          </div>
        {% endif %}

        {# Content Type Info Card #}
        <div class="mb-6 bg-blue-50 border border-blue-200 rounded-xl p-4">
          <div class="flex items-start space-x-3">
            <div class="flex-shrink-0">
              {% include '@intranet_megamidia/includes/icons.html.twig' with {'icon': 'plus', 'class': 'w-5 h-5 text-blue-600'} %}
            </div>
            <div>
              <h3 class="text-sm font-medium text-blue-900">{{ 'Criando'|t }} {{ node_type_label|default('Conteúdo'|t|lower) }}</h3>
              <div class="mt-1 text-sm text-blue-700">
                {% if node_type_label == 'Comunicado' %}
                  {{ 'Use este tipo de conteúdo para anúncios, avisos e comunicações importantes para toda a empresa.'|t }}
                {% elseif node_type_label == 'Documento' %}
                  {{ 'Para arquivos, manuais, políticas e documentos importantes que precisam ser organizados.'|t }}
                {% elseif node_type_label == 'Evento' %}
                  {{ 'Para agendar reuniões, treinamentos, eventos corporativos e outras atividades.'|t }}
                {% else %}
                  {{ 'Preencha as informações solicitadas abaixo.'|t }}
                {% endif %}
              </div>
            </div>
          </div>
        </div>

        {# Page Content - Form in Card #}
        <div class="animate-fade-in">
          <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
            {# Form Header with Actions #}
            <div class="px-6 py-4 border-b border-gray-100 bg-gray-50 flex items-center justify-between">
              <div class="flex items-center space-x-4">
                <div class="flex items-center space-x-2">
                  <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
                  <span class="text-sm text-gray-600">{{ 'Rascunho'|t }}</span>
                </div>
                <span class="text-sm text-gray-500">
                  {{ 'Não salvo'|t }}
                </span>
              </div>

              {# Quick Actions #}
              <div class="flex items-center space-x-3">
                <button type="button"
                        class="inline-flex items-center px-3 py-1.5 text-sm text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-lg transition-colors"
                        onclick="if(confirm('{{ 'Tem certeza que deseja cancelar? Suas alterações serão perdidas.'|t }}')) { window.history.back(); }">
                  {% include '@intranet_megamidia/includes/icons.html.twig' with {'icon': 'x', 'class': 'w-4 h-4 mr-1'} %}
                  {{ 'Cancelar'|t }}
                </button>
              </div>
            </div>

            {# Form Content #}
            <div class="p-6 md:p-8">
              {{ page.content }}
            </div>
          </div>
        </div>

      </div>
    </main>

    {# Footer #}
    {% if page.footer %}
      {% include '@intranet_megamidia/includes/footer.html.twig' %}
    {% endif %}

  </div>
</div>
