{#
/**
 * @file
 * Footer component template.
 *
 * Simple footer with copyright and useful links.
 */
#}

<footer class="bg-white border-t border-gray-200 py-6 px-4 md:px-8">
  <div class="max-w-7xl mx-auto">
    <div class="flex flex-col md:flex-row items-center justify-between gap-4">
      
      {# Copyright #}
      <div class="text-sm text-gray-500">
        © {{ "now"|date("Y") }} Megamidia. {{ 'Todos os direitos reservados.'|t }}
      </div>
      
      {# Footer Links #}
      <div class="flex items-center gap-6 text-sm">
        <a href="/politica-de-privacidade" class="text-gray-500 hover:text-gray-700 transition-colors">
          {{ 'Privacidade'|t }}
        </a>
        <a href="/termos-de-uso" class="text-gray-500 hover:text-gray-700 transition-colors">
          {{ 'Termos de Uso'|t }}
        </a>
        <a href="/suporte" class="text-gray-500 hover:text-gray-700 transition-colors">
          {{ 'Suporte'|t }}
        </a>
      </div>
      
      {# Footer Region (for blocks) #}
      {% if page.footer %}
        <div class="footer-blocks">
          {{ page.footer }}
        </div>
      {% endif %}
      
    </div>
  </div>
</footer>

